# LSM6DSV16X driver component — REUSED byte-identical from firmware/lite/lib/LSM6DSV16X
# (M2 host-verified). Keep identical to the Lite implementation; do not edit here.
idf_component_register(SRCS "lsm6dsv16x.c"
                       INCLUDE_DIRS "include")
